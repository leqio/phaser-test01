import {defineStore} from 'pinia';

export const useCounter = defineStore('user', {
    state: () => ({
        // name: '',
    }),
    actions: {
        // setName(name) {
        //     this.name = name;
        // },
    },
});